$('.answer-errors').html('<%= j render 'shared/errors', resource: @answer %>');
$('.container').prepend('<%= j render 'shared/flash', resource: @answer %>');
<% if @answer.persisted? %>
  $('.answers').append('<%= j render @answer %>');
  $('.new-answer #answer_body').val('');
  $('.no-answers').hide();
<% end %>
